# Swift Package Manager
.build/
.swiftpm/
# Package.resolved — tracked for reproducible CI builds

# Xcode
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcuserstate
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Rust
windows/target/
**/src-tauri/target/
# Sample app lockfile — example app, not a production binary.
# Omitted to avoid false-positive vulnerability alerts from transitive
# Linux-only deps (e.g. glib) that are never compiled on Windows.
Examples/windows/SampleApp/src-tauri/Cargo.lock

# Node / Tauri frontend
node_modules/
dist/
# package-lock.json — tracked for reproducible CI builds

# Claude Code
.claude/

# IDE
.vscode/
.idea/
